#!/bin/sh
set -eu

ROOT_DIR="$(CDPATH= cd -- "$(dirname "$0")/.." && pwd)"
PYTHON_BIN="$ROOT_DIR/.codex-test-venv/bin/python"

if [ ! -x "$PYTHON_BIN" ]; then
  echo "Missing test interpreter: $PYTHON_BIN" >&2
  exit 1
fi

exec "$PYTHON_BIN" -m pytest "$@"
